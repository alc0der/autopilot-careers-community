xdescribe("CLI Application", () => {
  it.todo("");
  // beforeEach(() => {
  //   jest.spyOn(process, "exit").mockImplementation((code) => {
  //     throw new Error(`process.exit called with "${code}"`);
  //   });
  // });
  // afterEach(() => {
  //   jest.clearAllMocks();
  // });
  // afterAll(() => {
  //   jest.restoreAllMocks();
  // });
  // it("should process the markdown file and call the processing function", async () => {
  //   const fs = require("fs");
  //   const path = require("path");
  //   const filePath = path.resolve(__dirname, "../out/3989104441.md");
  //   const fileContent = fs.readFileSync(filePath, "utf-8");
  //   const debugFilePath = path.resolve(
  //     __dirname,
  //     "../out/debug_fileContent.md",
  //   );
  //   fs.writeFileSync(debugFilePath, fileContent);
  //   console.log(`File content written to ${debugFilePath} for debugging.`);
  //   // Assuming processMarkdown is the function to process the file
  //   const processMarkdown = jest.fn();
  //   processMarkdown(fileContent);
  //   expect(processMarkdown).toHaveBeenCalledWith(fileContent);
  // });
});
