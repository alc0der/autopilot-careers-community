import { buildUrl } from "../src/browsers";

xdescribe("buildUrl", () => {
  it.todo("");
  // it("returns the provided URL when given", () => {
  //   expect(buildUrl("https://example.com", null)).toBe("https://example.com");
  // });
  // it("constructs LinkedIn URL when ID is provided without URL", () => {
  //   expect(buildUrl(null, "123456")).toBe(
  //     "https://www.linkedin.com/jobs/view/123456",
  //   );
  // });
  // it("returns undefined when both URL and ID are null", () => {
  //   expect(() => buildUrl(null, null)).toThrow();
  // });
  // it("prioritizes URL over ID when both are provided", () => {
  //   expect(buildUrl("https://example.com", "123456")).toBe(
  //     "https://example.com",
  //   );
  // });
});
