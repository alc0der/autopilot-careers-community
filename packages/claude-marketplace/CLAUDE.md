# write-resume-plugin

## Version Sync Rule

When bumping the version in any skill's SKILL.md, always bump `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json` to the same version. Before committing any version change, verify all three files show the same version string.
