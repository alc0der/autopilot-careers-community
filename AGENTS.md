# Autopilot Careers Monorepo

All repeatable operations (build, test, deploy, MCP setup, etc.) are defined as scripts in `package.json` (root and per-package). Check `package.json` before writing ad-hoc shell commands.
